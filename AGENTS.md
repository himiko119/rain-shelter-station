# 雨宿り駅の忘れもの — 自律開発規約

## 目的

Phaser 3 / TypeScript / Vite で、タイトルから3種類のエンディングまで遊べる2D探索アドベンチャーを完成させる。企画書や縦切りだけで終わらせず、実ブラウザで遊べる静的サイトとして出荷する。

## 優先順位

1. 最初から最後まで遊べること
2. 進行不能・セーブ破損・操作不能を起こさないこと
3. 物語・画面・音の一貫性
4. 単体テスト、E2E、実ブラウザ、スクリーンショットによる検証
5. 見た目と演出
6. 追加機能

## 実装規約

- ゲーム状態はPhaser Sceneの外に置き、シリアライズ可能なプレーンデータと純粋な遷移で管理する。
- Sceneは描画・カメラ・衝突・入力の中継に限定し、DOMは会話、ノート、所持品、ポーズ、設定、記録を担当する。
- 入力は物理キーやポインターから `move`、`interact`、`open-note`、`pause` 等のアクションへ変換する。
- コンテンツは安定IDを持つデータとして管理する。Sceneへ大量の会話や進行条件を書かない。
- アセットはmanifestキーで参照し、ゲームロジックへパスを直書きしない。
- localStorageはバージョン付きで検証する。破損・未知ID・将来版を安全に拒否または復旧する。
- 重要アイテムは消失させず、誤返却は段階ヒントへ変換する。全アイテムを後から回収可能にする。
- 本番へ秘密情報、任意state注入、開発デバッグUIを含めない。
- 外部権利物を模倣せず、図形・SVG・Web Audioでオリジナル素材を作る。
- 不要な `innerHTML`、未破棄イベント、無制限パーティクル、過剰な点滅や画面揺れを避ける。

## 品質基準

- 未完成ボタン、仮画面、ダミー処理、TODO、Coming Soonを残さない。
- キーボード、ポインター、390px級のタッチ画面で主要操作が可能であること。
- フォーカス表示、ミュート、音量、文字速度、一括表示、演出軽減を提供する。
- 1280x720と390x844で文字切れ、横スクロール、UI重なり、暗すぎる画面がないこと。
- ページエラー、未処理例外、重大なconsole errorをE2E失敗として扱う。

## 必須検証

- `npm run lint`
- `npm run typecheck`
- `npm run test`
- `npm run test:e2e`
- `npm run build`
- `npm run check`
- 通常入力だけで最初の忘れものを返す短いブラウザルート
- セーブ復元、誤返却、段階ヒント、3エンディング、データ削除
- `artifacts/playtest` の主要10画面を実際に目視確認

## Git

生成物と `node_modules` をコミットしない。フェーズ単位で差分とテスト結果を確認し、意味のあるコミットを作る。既存の無関係な変更が見つかった場合は上書きしない。
